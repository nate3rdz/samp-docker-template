EASY CONFIG:

1) sudo docker build -t samp-server .
2) sudo docker run -p 7777:7777 -p 7777:7777/udp --name samp-server samp-server


be sure, once you want to restart, to do
3) sudo docker rm samp-server

then repeat the commands written above.
